# BCI E-Library - React Native eBook Library Project Documentation


## ১. প্রজেক্ট ওভারভিউ (Project Overview)

**BCI E-Library** একটি React Native CLI দিয়ে তৈরি করা eBook লাইব্রেরি অ্যাপ্লিকেশন। এই অ্যাপে ব্যবহারকারীরা:
• বই খুঁজতে পারে এবং পড়তে পারে
• পছন্দের বইগুলো ফেভারিটে সেভ করতে পারে  
• ডার্ক/লাইট থিম ব্যবহার করতে পারে
• appwrite ব্যাকএন্ড সার্ভিস ব্যবহার করে ডেটা স্টোর করে
• PDF ফাইল আপলোড এবং ভিউ করতে পারে

## ২. ফোল্ডার স্ট্রাকচার (Folder Structure)

**📁 android/** - Android অ্যাপ বিল্ড কনফিগারেশন
**📁 ios/** - iOS অ্যাপ বিল্ড কনফিগারেশন  
**📁 screens/** - অ্যাপের সব স্ক্রিন (HomeScreen, AuthScreen, etc.)
**📁 components/** - পুনর্ব্যবহারযোগ্য কম্পোনেন্ট (CustomTabBar, FilterModal)
**📁 services/** - API সার্ভিস ফাংশন (bookService, authService)
**📁 context/** - React Context API (AuthContext, ThemeContext)
**📁 hooks/** - কাস্টম React Hooks (useAuth, useBookFilters)
**📁 lib/** - লাইব্রেরি কনফিগারেশন (appwrite.js)
**📁 constants/** - কনস্ট্যান্ট ভ্যালু (Config.js)

## ৩. মূল কম্পোনেন্টসমূহ (Key Components)

### App.jsx (মূল অ্যাপ কম্পোনেন্ট)
- **কি করে:** অ্যাপের মূল এন্ট্রি পয়েন্ট এবং রুটিং সিস্টেম
- **কেন ব্যবহার:** সব প্রোভাইডার (ThemeProvider, AuthProvider) এবং নেভিগেশন সেটআপ করে
- **কিভাবে কাজ করে:** 
  • Context API দিয়ে থিম এবং অথেনটিকেশন স্টেট ম্যানেজ করে
  • Stack Navigator দিয়ে স্ক্রিন নেভিগেশন করে
  • ইউজার লগইন স্টেট অনুযায়ী স্ক্রিন দেখায় (Auth/VerifyEmail/MainTabs)
- **গুরুত্বপূর্ণ কোড:**
```javascript
export default function App() {
  return (
    <ThemeProvider>
      <AuthProvider>
        <AppContent />
      </AuthProvider>
    </ThemeProvider>
  );
}
```

### AuthContext.jsx (অথেনটিকেশন কনটেক্সট)
- **কি করে:** পুরো অ্যাপে ইউজার অথেনটিকেশন স্টেট ম্যানেজ করে
- **কেন ব্যবহার:** 
  • ইউজার লগইন/লগআউট স্টেট ট্র্যাক করার জন্য
  • সব স্ক্রিনে ইউজার ডেটা শেয়ার করার জন্য
  • Props drilling এড়ানোর জন্য
- **কিভাবে কাজ করে:**
  • Appwrite API কল করে ইউজার ডেটা ফেচ করে
  • useState দিয়ে user এবং loading স্টেট ম্যানেজ করে
  • login, logout, register ফাংশন প্রদান করে
  • useEffect দিয়ে অ্যাপ শুরুতে ইউজার চেক করে
- **গুরুত্বপূর্ণ ফাংশন:**
```javascript
const login = useCallback(async (email, password) => {
  await account.createEmailPasswordSession(email, password);
  const currentUser = await account.get();
  setUser(mapAppwriteUser(currentUser));
});
```

### HomeScreen.jsx (হোম স্ক্রিন)
- **কি করে:** অ্যাপের মূল স্ক্রিন যেখানে বইয়ের লিস্ট দেখায়
- **কেন ব্যবহার:** 
  • ইউজার যাতে বই খুঁজতে পারে
  • সার্চ এবং ফিল্টার করতে পারে
  • বইয়ের বিস্তারিত দেখতে পারে
- **কিভাবে কাজ করে:**
  • bookService থেকে ডেটা লোড করে
  • FlatList দিয়ে বইয়ের লিস্ট দেখায়
  • useState দিয়ে books, loading, query স্টেট ম্যানেজ করে
  • FilterModal দিয়ে ফিল্টার অপশন দেয়
  • useFocusEffect দিয়ে স্ক্রিন ফোকাসে ডেটা রিফ্রেশ করে
- **গুরুত্বপূর্ণ স্টেট:**
```javascript
const [books, setBooks] = useState([]);
const [loading, setLoading] = useState(false);
const [query, setQuery] = useState('');
const [filterOpen, setFilterOpen] = useState(false);
```

### CustomTabBar.jsx (কাস্টম ট্যাব বার)
- **কি করে:** অ্যাপের নিচের কাস্টম ট্যাব বার তৈরি করে
- **কেন ব্যবহার:** 
  • সুন্দর UI এবং স্মুথ অ্যানিমেশন
  • ডার্ক/লাইট থিম সাপোর্ট
  • ইউজার এক্সপেরিয়েন্স উন্নত করার জন্য
- **কিভাবে কাজ করে:**
  • Animated API দিয়ে ট্যাব ইনডিকেটর অ্যানিমেট করে
  • useRef দিয়ে অ্যানিমেশন ভ্যালু স্টোর করে
  • Dimensions API দিয়ে স্ক্রিন সাইজ পায়
  • Ionicons দিয়ে আইকন দেখায়
- **গুরুত্বপূর্ণ ফিচার:**
```javascript
const indicatorAnim = useRef(new Animated.Value(0)).current;
Animated.spring(indicatorAnim, {
  toValue: targetPosition,
  useNativeDriver: false,
  friction: 7,
  tension: 80,
}).start();
```

### BookDetailsScreen.jsx (বইয়ের বিস্তারিত স্ক্রিন)
- **কি করে:** নির্বাচিত বইয়ের বিস্তারিত তথ্য দেখায়
- **কেন ব্যবহার:** ইউজার যাতে বই সম্পর্কে সব তথ্য জানতে পারে
- **কিভাবে কাজ করে:**
  • route.params থেকে bookId নেয়
  • bookService থেকে বইয়ের ডেটা লোড করে
  • PDF ভিউয়ারে নেভিগেট করার অপশন দেয়
  • ফেভারিটে যোগ করার ফিচার দেয়

### PdfViewerScreen.jsx (PDF ভিউয়ার স্ক্রিন)
- **কি করে:** PDF ফাইল ভিউ করার জন্য
- **কেন ব্যবহার:** ইউজার যাতে বই পড়তে পারে
- **কিভাবে কাজ করে:**
  • react-native-pdf লাইব্রেরি ব্যবহার করে
  • Appwrite Storage থেকে PDF ফাইল লোড করে
  • পেজ নেভিগেশন এবং জুম ফিচার দেয়

### AuthScreen.jsx (অথেনটিকেশন স্ক্রিন)
- **কি করে:** লগইন এবং রেজিস্ট্রেশন ফর্ম দেখায়
- **কেন ব্যবহার:** ইউজার অ্যাকাউন্ট তৈরি এবং লগইন করার জন্য
- **কিভাবে কাজ করে:**
  • useState দিয়ে ফর্ম স্টেট ম্যানেজ করে
  • AuthContext এর login/register ফাংশন কল করে
  • এরর মেসেজ এবং লোডিং স্টেট দেখায়
  • useMemo দিয়ে title এবং subtitle অপটিমাইজ করে
  • KeyboardAvoidingView দিয়ে কীবোর্ড হ্যান্ডল করে
- **গুরুত্বপূর্ণ ফিচার:**
  • **ডুয়াল মোড:** একই স্ক্রিনে লগইন এবং রেজিস্ট্রেশন
  • **পাসওয়ার্ড ভিজিবিলিটি:** আইকন দিয়ে পাসওয়ার্ড দেখানো/লুকানো
  • **পাসওয়ার্ড ম্যাচিং:** রেজিস্ট্রেশনে পাসওয়ার্ড ম্যাচ চেক
  • **ফর্ম ভ্যালিডেশন:** সব ফিল্ড চেক এবং এরর মেসেজ
  • **লোডিং স্টেট:** বাটন ডিসেবল এবং লোডিং টেক্সট
- **গুরুত্বপূর্ণ স্টেট:**
```javascript
const [mode, setMode] = useState(initialMode); // login/register
const [email, setEmail] = useState('');
const [password, setPassword] = useState('');
const [name, setName] = useState('');
const [showPassword, setShowPassword] = useState(false);
const [loading, setLoading] = useState(false);
```
- **গুরুত্বপূর্ণ ফাংশন:**
```javascript
const handleSubmit = async () => {
  // ফর্ম ভ্যালিডেশন
  if (!trimmedEmail || !trimmedPassword || (!isLogin && !trimmedName)) {
    Alert.alert('Error', 'Please fill in all fields');
    return;
  }
  
  // পাসওয়ার্ড ম্যাচ চেক
  if (!isLogin && trimmedPassword !== trimmedConfirmPassword) {
    Alert.alert('Error', 'Passwords do not match');
    return;
  }
  
  // API কল
  const result = isLogin
    ? await login(trimmedEmail, trimmedPassword)
    : await register(trimmedEmail, trimmedPassword, trimmedName);
};
```
- **UI কম্পোনেন্ট:**
  • **TextInput:** ইমেইল, পাসওয়ার্ড, নাম ইনপুট
  • **TouchableOpacity:** বাটন এবং টগল
  • **Ionicons:** আইকন (মেইল, লক, আই, চেকমার্ক)
  • **Alert:** এরর মেসেজ প্রদর্শন
  • **Image:** হেডার লোগো
- **স্টাইলিং ফিচার:**
  • **শ্যাডো:** ইনপুট ফিল্ডে elevation/shadow
  • **রাউন্ডেড কর্নার:** borderRadius: 12
  • **রেসপন্সিভ:** KeyboardAvoidingView
  • **থিম:** লাইট থিম কালার স্কিম

### FilterModal.jsx (ফিল্টার মোডাল)
- **কি করে:** বই ফিল্টার করার অপশন দেয়
- **কেন ব্যবহার:** ইউজার যাতে সহজে বই খুঁজতে পারে
- **কিভাবে কাজ করে:**
  • Modal কম্পোনেন্ট ব্যবহার করে
  • ক্যাটাগরি, ভাষা, প্রকাশক ইত্যাদি ফিল্টার দেয়
  • useBookFilters হুক ব্যবহার করে

### ThemeContext.jsx (থিম কনটেক্সট)
- **কি করে:** ডার্ক/লাইট থিম স্টেট ম্যানেজ করে
- **কেন ব্যবহার:** ইউজার প্রেফারেন্স অনুযায়ী থিম পরিবর্তন
- **কিভাবে কাজ করে:**
  • AsyncStorage দিয়ে থিম সেটিংস সেভ করে
  • useState দিয়ে currentTheme স্টেট ম্যানেজ করে
  • সব কম্পোনেন্টে থিম ডেটা প্রদান করে

## ৪. Node Modules ও Packages

### মূল ডিপেন্ডেন্সি:

**@react-navigation/native** (^7.1.17)
- **কি করে:** স্ক্রিন নেভিগেশন
- **কেন প্রয়োজন:** এক স্ক্রিন থেকে অন্য স্ক্রিনে যাওয়ার জন্য
- **ব্যবহার:** `NavigationContainer` এবং `Stack.Navigator`

**@react-navigation/bottom-tabs** (^7.4.6)
- **কি করে:** নিচের ট্যাব নেভিগেশন
- **কেন প্রয়োজন:** হোম, ফেভারিট, সেটিংস ট্যাব
- **ব্যবহার:** `createBottomTabNavigator()`

**appwrite** (^18.2.0)
- **কি করে:** ব্যাকএন্ড API সার্ভিস
- **কেন প্রয়োজন:** ডেটা স্টোর এবং অথেনটিকেশন
- **ব্যবহার:** `Client`, `Account`, `Databases`, `Storage`

**react-native-pdf** (^6.7.7)
- **কি করে:** PDF ফাইল ভিউ
- **কেন প্রয়োজন:** বই পড়ার জন্য
- **ব্যবহার:** `PdfViewerScreen` এ

**@react-native-async-storage/async-storage** (^2.2.0)
- **কি করে:** লোকাল ডেটা স্টোর
- **কেন প্রয়োজন:** ইউজার প্রেফারেন্স সেভ
- **ব্যবহার:** থিম সেটিংস সেভ

**react-native-vector-icons** (^10.3.0)
- **কি করে:** আইকন প্রদর্শন
- **কেন প্রয়োজন:** UI আইকন
- **ব্যবহার:** সব স্ক্রিনে আইকন দেখানোর জন্য

## ৫. মূল React Native কনসেপ্ট

### State Management (স্টেট ম্যানেজমেন্ট)
```javascript
const [books, setBooks] = useState([]);
const [loading, setLoading] = useState(false);
```
**ব্যাখ্যা:** কম্পোনেন্টের ডেটা স্টোর করে, যখন পরিবর্তন হয় তখন UI আপডেট হয়

### Props (প্রপস)
```javascript
export default function HomeScreen({ navigation }) {
```
**ব্যাখ্যা:** প্যারেন্ট থেকে চাইল্ড কম্পোনেন্টে ডেটা পাস করে

### Navigation (নেভিগেশন)
```javascript
navigation.navigate('BookDetails', { bookId: book.id });
```
**ব্যাখ্যা:** এক স্ক্রিন থেকে অন্য স্ক্রিনে যাওয়ার সিস্টেম

### Context API (কনটেক্সট API)
```javascript
const { user } = useAuth();
const { theme } = useTheme();
```
**ব্যাখ্যা:** পুরো অ্যাপে ডেটা শেয়ার করার জন্য

### Custom Hooks (কাস্টম হুকস)
```javascript
export const useAuth = () => {
  const ctx = useContext(AuthContext);
  return ctx;
};
```
**ব্যাখ্যা:** লজিক পুনর্ব্যবহার করার জন্য

### Async/Await (অ্যাসিঙ্ক/অ্যাওয়েট)
```javascript
const loadBooks = async () => {
  try {
    const result = await listBooks();
    setBooks(result);
  } catch (error) {
    console.error(error);
  }
};
```
**ব্যাখ্যা:** API কল করার জন্য অ্যাসিঙ্ক্রোনাস প্রোগ্রামিং

## ৬. সাধারণ পরীক্ষার প্রশ্ন ও উত্তর

### প্রশ্ন: কেন useState ব্যবহার করা হয়েছে?
**উত্তর:** কম্পোনেন্টের ডেটা স্টোর করার জন্য। যখন ডেটা পরিবর্তন হয়, React অটোমেটিক UI আপডেট করে।

### প্রশ্ন: Appwrite কি এবং কেন ব্যবহার করা হয়েছে?
**উত্তর:** Appwrite একটি Backend-as-a-Service (BaaS)। এটি ব্যবহার করা হয়েছে কারণ:
- ডেটাবেস ম্যানেজমেন্ট সহজ
- অথেনটিকেশন সিস্টেম আছে
- ফাইল স্টোরেজ আছে
- API কল সহজ

### প্রশ্ন: Context API কেন ব্যবহার করা হয়েছে?
**উত্তর:** পুরো অ্যাপে ডেটা শেয়ার করার জন্য। Props drilling এড়ানোর জন্য।

### প্রশ্ন: CustomTabBar ফাইল কেন গুরুত্বপূর্ণ?
**উত্তর:** এটি অ্যাপের নিচের নেভিগেশন বার তৈরি করে। এটি না থাকলে ইউজার স্ক্রিন পরিবর্তন করতে পারবে না।

### প্রশ্ন: যদি AuthContext সরিয়ে দেওয়া হয় তাহলে কি হবে?
**উত্তর:** ইউজার লগইন স্টেট ট্র্যাক করা যাবে না, সব স্ক্রিনে লগইন চেক করতে হবে।

### প্রশ্ন: useAuth হুক কি করে?
**উত্তর:** AuthContext থেকে ইউজার ডেটা এবং ফাংশন (login, logout) পেতে সাহায্য করে।

### প্রশ্ন: FlatList কেন ব্যবহার করা হয়েছে?
**উত্তর:** বড় লিস্টের জন্য পারফরম্যান্স ভালো। ScrollView এর চেয়ে মেমরি কম খরচ করে।

### প্রশ্ন: AsyncStorage কি কাজে লাগে?
**উত্তর:** ডিভাইসে ডেটা সেভ করে। অ্যাপ বন্ধ হওয়ার পরেও ডেটা থাকে।

## ৭. বেস্ট প্র্যাকটিস

### কোড অরগানাইজেশন:
• **ফোল্ডার স্ট্রাকচার:** প্রতিটি ফাংশনালিটি আলাদা ফোল্ডারে
• **ফাইল নেমিং:** ক্যামেলকেস (camelCase) ব্যবহার
• **কম্পোনেন্ট সেপারেশন:** ছোট ছোট কম্পোনেন্টে ভাগ

### স্টেট ম্যানেজমেন্ট:
• **Context API:** গ্লোবাল স্টেটের জন্য
• **useState:** লোকাল স্টেটের জন্য
• **useEffect:** সাইড ইফেক্টের জন্য

### এরর হ্যান্ডলিং:
```javascript
try {
  const result = await apiCall();
} catch (error) {
  console.error('Error:', error);
}
```

### পারফরম্যান্স:
• **FlatList:** বড় লিস্টের জন্য
• **useMemo:** কস্টলি ক্যালকুলেশনের জন্য
• **useCallback:** ফাংশন মেমোইজেশনের জন্য

### সিকিউরিটি:
• **API Keys:** কনফিগ ফাইলে সেভ
• **টোকেন ম্যানেজমেন্ট:** সেশন টোকেন সেভ
• **ইনপুট ভ্যালিডেশন:** ইউজার ইনপুট চেক

## ৮. প্রজেক্ট রান করার নিয়ম

```bash
# ডিপেন্ডেন্সি ইনস্টল
npm install

# Android এর জন্য
npm run android

# iOS এর জন্য  
npm run ios

# মেট্রো সার্ভার
npm start
```

## ৯. গুরুত্বপূর্ণ ফাইলসমূহ

1. **App.jsx** - মূল অ্যাপ এন্ট্রি
2. **constants/Config.js** - অ্যাপরাইট কনফিগারেশন
3. **lib/appwrite.js** - অ্যাপরাইট ক্লায়েন্ট
4. **context/AuthContext.jsx** - অথেনটিকেশন লজিক
5. **services/bookService.js** - বই API কল
6. **screens/HomeScreen.jsx** - মূল স্ক্রিন

## ১০. পরীক্ষার জন্য গুরুত্বপূর্ণ পয়েন্ট

• **React Native CLI vs Expo:** CLI ব্যবহার করা হয়েছে
• **Navigation:** React Navigation ব্যবহার
• **State Management:** Context API + useState
• **Backend:** Appwrite BaaS
• **File Storage:** PDF এবং ইমেজ স্টোর
• **Authentication:** Email/Password + JWT
• **UI Components:** Custom TabBar, Modal, FlatList
• **Error Handling:** Try-catch ব্লক
• **Performance:** useMemo, useCallback, FlatList
• **Security:** API key protection, session management

এই ডকুমেন্টেশন আপনার পরীক্ষার জন্য যথেষ্ট। প্রতিটি কনসেপ্ট সহজ ভাষায় ব্যাখ্যা করা হয়েছে।
